{
  "name": "class-7-12-learning-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
